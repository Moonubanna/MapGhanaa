import { connect } from 'react-redux'

import Indivisuals from '../../components/Screens/Indivisuals'
import {

} from '../../thunks'

const mapStateToProps = ({ AppUsers  }) => ({

})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(Indivisuals)